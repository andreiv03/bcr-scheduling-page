export interface Unit {
  appointmentsSchedule: {
    isCashless: boolean;
  };
  br_street: string;
  branchId: string;
  brn: string;
  distance: number;
  location: {
    latitude: string;
    longitude: string;
  };
  mfm_euro_all_day: boolean;
}
